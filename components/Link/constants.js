const DEFAULT = "transition-colors";

const SIZE = {
  sm: "text-sm",
  base: "text-base",
  md: "text-md",
  lg: "text-lg",
  xl: "text-xl",
};

const STYLE = {
  primary: "text-white hover:text-white",
  secondary: "text-black hover:text-sky-400",
};

export { DEFAULT, SIZE, STYLE };
